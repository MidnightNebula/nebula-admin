## 📘 How to Use

To ensure the application works correctly, you must set the API server URL in the environment variables.

### Setup dependencies

`pnpm i`

### Start the project

`pnpm dev`

### 1️⃣ Specify the API URL in `.env`

Create a `.env` file (if it doesn’t exist) and copy variables from `ci/.env.example`
